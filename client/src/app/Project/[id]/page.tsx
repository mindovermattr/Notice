"use client";

const page = ({
  params,
}: {
  params: Promise<{
    id: string;
  }>;
}) => {
  // const router = useRouter();
  // const { id } = use(params);
  // useEffect(() => {
  //   router.push(`${id}/Dashboard/Tasklist`);
  // }, []);
  return <div>wqeqwe</div>;
};

export default page;
